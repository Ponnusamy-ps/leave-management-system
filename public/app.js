const loginForm=document.getElementById("loginForm"),signupForm=document.getElementById("signupForm"),loginMessage=document.getElementById("loginMessage"),signupMessage=document.getElementById("signupMessage"),loginBox=document.getElementById("loginBox"),signupBox=document.getElementById("signupBox"),loginTab=document.getElementById("loginTab"),signupTab=document.getElementById("signupTab");
if(localStorage.getItem("lms_token")) window.location.href="/dashboard.html";
function showLogin(){loginBox.classList.remove("hidden");signupBox.classList.add("hidden");loginTab.classList.add("active");signupTab.classList.remove("active");loginMessage.textContent="";signupMessage.textContent="";}
function showSignup(){loginBox.classList.add("hidden");signupBox.classList.remove("hidden");loginTab.classList.remove("active");signupTab.classList.add("active");loginMessage.textContent="";signupMessage.textContent="";}
loginTab.addEventListener("click",showLogin);signupTab.addEventListener("click",showSignup);
loginForm.addEventListener("submit",async e=>{e.preventDefault();const username=document.getElementById("username").value.trim(),password=document.getElementById("password").value,role=document.querySelector('input[name="access"]:checked').value;loginMessage.textContent="Checking login...";loginMessage.className="message";try{const r=await fetch("/api/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({username,password,role})}),d=await r.json();if(!r.ok)throw new Error(d.message||"Login failed.");localStorage.setItem("lms_token",d.token);localStorage.setItem("lms_user",JSON.stringify(d.user));loginMessage.textContent="Login successful. Opening dashboard...";loginMessage.className="message success";setTimeout(()=>location.href="/dashboard.html",250);}catch(err){loginMessage.textContent=err.message;loginMessage.className="message error";}});
signupForm.addEventListener("submit",async e=>{e.preventDefault();const fullName=document.getElementById("fullName").value.trim(),email=document.getElementById("email").value.trim(),username=document.getElementById("signupUsername").value.trim(),password=document.getElementById("signupPassword").value,confirmPassword=document.getElementById("confirmPassword").value;if(password!==confirmPassword){signupMessage.textContent="Passwords do not match.";signupMessage.className="message error";return;}signupMessage.textContent="Creating account...";signupMessage.className="message";try{const r=await fetch("/api/signup",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({fullName,email,username,password})}),d=await r.json();if(!r.ok)throw new Error(d.message||"Signup failed.");signupMessage.textContent=d.message;signupMessage.className="message success";signupForm.reset();setTimeout(()=>{showLogin();document.getElementById("username").value=username;document.getElementById("password").focus();},800);}catch(err){signupMessage.textContent=err.message;signupMessage.className="message error";}});
